{
  "name": "@soundwala/spur-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "SPUR dashboard — thin local web server rendering the engine's index (surface B).",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@soundwala/spur": "*"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
